<h1><%= link_to @actor.name, @actor %></h1><br>
<h4>Appeared in: </h4>
<div>
  <span><%= render @actor.videos %></span><br/>

 <% if current_user && current_user.admin? %>
    <%= link_to 'Edit', edit_actor_path(@actor) %> |
    <%= link_to 'Back', actors_path %>
 <% end %>
</div>
