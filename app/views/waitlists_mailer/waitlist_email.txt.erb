Hey, your waitlisted item is now available
===============================================

         Item: 
                    <%= @video.name %>
                    <%= image_tag(@video.thumbnail, size: '190x165') if @video.thumbnail.present? %>
	            price: <%= @video.rent_price*2 %>
                    Newly available items stock: <%= @video.stock %>

You can go shopping now!
